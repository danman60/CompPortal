# Chatwoot Environment Configuration
# Copy this file to .env and fill in your actual values

# ==============================================================================
# DATABASE & CACHE PASSWORDS
# ==============================================================================
# Generate strong random passwords:
# openssl rand -hex 32

POSTGRES_PASSWORD=your_strong_postgres_password_here
REDIS_PASSWORD=your_strong_redis_password_here

# ==============================================================================
# APPLICATION SECRET
# ==============================================================================
# Generate with: openssl rand -hex 64
SECRET_KEY_BASE=your_64_character_secret_key_base_here

# ==============================================================================
# FRONTEND URL
# ==============================================================================
# The public URL where Chatwoot will be accessible
# If using subdomain: https://chat.glowdance.com
# If using droplet IP: http://YOUR_DROPLET_IP:3000
FRONTEND_URL=https://chat.glowdance.com

# ==============================================================================
# EMAIL CONFIGURATION (SMTP - Sending emails)
# ==============================================================================
# Use your email provider's SMTP settings
# Examples:
#   Gmail: smtp.gmail.com, port 587
#   SendGrid: smtp.sendgrid.net, port 587
#   Mailgun: smtp.mailgun.org, port 587
#   AWS SES: email-smtp.us-east-1.amazonaws.com, port 587

MAILER_SENDER_EMAIL=support@glowdance.com
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=support@glowdance.com
SMTP_PASSWORD=your_smtp_password_or_app_specific_password

# ==============================================================================
# EMAIL CONFIGURATION (IMAP - Receiving reply emails)
# ==============================================================================
# Same email provider's IMAP settings
# Examples:
#   Gmail: imap.gmail.com, port 993
#   Outlook: outlook.office365.com, port 993

IMAP_ADDRESS=imap.gmail.com
IMAP_PORT=993
IMAP_EMAIL=support@glowdance.com
IMAP_PASSWORD=your_imap_password_or_app_specific_password

# ==============================================================================
# NOTES:
# ==============================================================================
# 1. For Gmail, you need to create an "App Password" (not your regular password)
#    Go to: Google Account > Security > 2-Step Verification > App Passwords
#
# 2. Make sure to enable IMAP in Gmail settings:
#    Gmail > Settings > Forwarding and POP/IMAP > Enable IMAP
#
# 3. For production, consider using a dedicated email service like SendGrid or Mailgun
#
# 4. Never commit the actual .env file to git - it contains secrets!
