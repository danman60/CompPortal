import { EntriesPageContainer } from '@/components/rebuild/entries/EntriesPageContainer';

/**
 * BETA: Entries page for testing before Nov 8 launch
 * Accessible via direct URL only (not linked from dashboard)
 * Share URL: empwr.compsync.net/dashboard/entries-beta
 */
export default function EntriesBetaPage() {
  return <EntriesPageContainer />;
}
